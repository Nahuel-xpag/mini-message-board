<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini message board</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1><%= title %></h1>
    <main>
        <ul>
            <div class="all-comments">
                <% messages.forEach(message => {  %>
                    <div class="comment">
                        <h4><%= message.user %></h4>
                        <p><%= message.text %></p>
                        <p>- <%= message.added %></p>
                        <button>See full comment</button>
                        </div>
                   <% }) %> 
            </div>
        </ul>
       <a href="/new">Submit a message here</a>
    </main>
</body>
</html>